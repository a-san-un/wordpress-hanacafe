---
version: 3.3.1
last_updated: 2026-04-08
status: このドキュメントはHanaCAFE本番公開前後の品質確認手順の唯一の情報源です。
---

# HanaCAFE 品質チェックリスト

HanaCAFE サイトの本番公開前後に実施する確認手順です。
[本番移行手順書.md](08_%E6%9C%AC%E7%95%AA%E7%A7%BB%E8%A1%8C%E6%89%8B%E9%A0%86%E6%9B%B8.md) とあわせて使用します。

---

## 1. 事前準備

### 使用ブラウザ・環境

- Google Chrome 最新版
- TOP確認用URL  
  - 本番: `https://intp.site/2999/WordPress/hanacafe`
  - ローカル: `http://hanacafe.local`
- WordPress 管理画面にログイン済み
- Gravatar画像による `imgNoAlt` の誤検出を考慮する
- DevTools は PC 幅 1280px と SP 幅 375px の両方で確認する
- Chrome DevTools の Console を使用できること
- `3-4 noindex確認` では `location.hostname` を利用する

### プラグイン確認

- ACF
- Yoast SEO
- SEO / XML / OGP 設定
- `noindex`
- CPT UI
- CPT
- All-in-One WP Migration
- WP Super Cache

---

## 2. 表示確認

### 2-1. レイアウト

- SP / PC の表示崩れがない
- Hero / About / Menu / Access / News / Footer が表示される
- Menu は 3カテゴリで表示される
- ドロワーメニューが開閉できる

### 2-2. 操作確認

- リンク切れがない
- 画像が正しく表示される
- Tab 移動が破綻しない

---

## 3. Chrome DevTools Console 実行項目

- Console エラーが 0 件
- TOPページで実行する

### 3-1. SEO確認

```js
const seo = {
  title: document.title,
  description: document.querySelector('meta[name="description"]')?.content ?? '',
  canonical: document.querySelector('link[rel="canonical"]')?.href ?? '',
  robots: document.querySelector('meta[name="robots"]')?.content ?? '',
  ogTitle: document.querySelector('meta[property="og:title"]')?.content ?? '',
  ogImage: document.querySelector('meta[property="og:image"]')?.content ?? '',
  twitterCard: document.querySelector('meta[name="twitter:card"]')?.content ?? '',
  h1Count: document.querySelectorAll('h1').length,
  h2Count: document.querySelectorAll('h2').length,
  imgTotal: document.querySelectorAll('img').length,
  imgNoAlt: [...document.querySelectorAll('img')].filter(i => !i.alt.length).length,
  viewport: document.querySelector('meta[name="viewport"]')?.content ?? '',
};
console.table(seo);
```

- `h1Count = 1`
- `imgNoAlt = 0`（Gravatar を除く）

### 3-2. DOM存在確認

```js
const checks = {
  hero: '.p-hero',
  menu: '.p-menu',
  seats: '.p-about',
  access: '.p-access',
  footer: '.l-footer',
  drawer: '.p-drawer',
  hamburger: '.l-header__hamburger',
};
Object.entries(checks).forEach(([name, sel]) => console.log(name, !!document.querySelector(sel)));
```

### 3-3. 壊れ画像確認

```js
const broken = [...document.querySelectorAll('img')]
  .filter(i => !i.complete || i.naturalWidth === 0);
console.log(broken.length);
broken.forEach(i => console.log(i.src));
```

### 3-4. noindex確認

```js
const base = location.hostname === 'hanacafe.local'
  ? 'http://hanacafe.local'
  : 'https://intp.site/2999/WordPress/hanacafe';

const pages = [
  `${base}/common-info`,
  `${base}/access-info`,
  `${base}/menu-info`,
  `${base}/about-seats`,
  `${base}/news-info`,
];

Promise.all(
  pages.map(url =>
    fetch(url)
      .then(r => r.text())
      .then(html => {
        const m = html.match(/<meta name="robots" content="([^"]+)"/i);
        return `${url} => ${m ? m[1] : 'robots metaなし'}`;
      })
  )
).then(r => console.log(r.join('\n')));
```

### 3-5. robots.txt確認

```js
const base = location.hostname === 'hanacafe.local'
  ? 'http://hanacafe.local'
  : 'https://intp.site/2999/WordPress/hanacafe';
fetch(`${base}/robots.txt`).then(r => r.text()).then(console.log);
```

---

_最終更新: 2026-04-08 | v3.3.1 | HanaCAFE_
