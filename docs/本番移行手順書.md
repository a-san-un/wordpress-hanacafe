---
version: 1.0.1
last_updated: 2026-04-08
status: このドキュメントはHanaCAFE本番移行と環境切替手順の唯一の情報源です。
---

# HanaCAFE 本番移行手順書

HanaCAFE WordPress サイトをローカル環境から本番環境へ移行するための手順書です。
WordPress本体・テーマ・プラグイン・ACF/SEO設定を安全に引き継ぐことを目的とします。

---

## 1. 前提条件

### 1.1 サーバー要件

- PHP 8.0 以上（PHP 8.0 関数 `str_starts_with` を使用）
- PHP拡張: `fileinfo`, `mbstring`

### 1.2 WordPress バージョン

- WordPress 6.3 以上（`wp_enqueue_script()` の `strategy` 引数対応）
- 推奨: 6.9 系

### 1.3 Node.js / npm

- Node.js 18 以上
- `npm install` 実行済み
- `npm run lint` でエラーなし

---

## 2. 事前確認

### 2.1 ACF 設定

以下の固定ページが存在し、ACF設定が投入されていることを確認する。

- `common-info`
- `access-info`
- `menu-info`
- `about-seats`

確認対象:
- Hero: `pic`
- fallback: `site_default_image`
- Access: `shop_*`, `seat_check_url`, `shop_map_*`
- Menu: `top_menu_*`

### 2.2 CPT / Taxonomy

- post type: `menu`
- taxonomy: `menu_category`
- `menu_category` の slug は `meal`, `drink`, `dessert`
- `get_hanacafe_menu_categories()`（`functions.php`）と一致していること
- 各カテゴリーに最低1件以上のメニューがあること

---

## 3. ファイル確認

### 3.1 必須アセット

- `assets/css/app.css`
- `assets/js/main.js`
- `assets/images/coming-soon.jpg`
- `assets/images/map.png`
- `assets/images/hero.jpg`

### 3.2 外部依存

- `fonts.googleapis.com`
- `fonts.gstatic.com`
- CSP / Firewall で Google Fonts が遮断されないこと

---

## 4. 動作確認

### 4.1 ナビゲーション

- `global-nav`
- `drawer-nav`
- `Esc` でドロワーが閉じること

### 4.2 画像

- 画像切れがないこと
- alt が空でないこと

### 4.3 テンプレート

- home は `archive-post.php` を使用すること
- menu は `posts_per_page = -1` で全件表示されること

### 4.4 メニュー表示

- Menu セクションおよびアーカイブで `meal` / `drink` / `dessert` の3カテゴリが表示されること
- トップページの `top_menu_meal` / `top_menu_drink` / `top_menu_dessert` が正しく表示されること

---

## 5. セキュリティ・品質

### 5.1 エスケープ

- `esc_html`, `esc_attr`, `esc_url` の適用確認
- Access の一部HTMLは `wp_kses_post` を使用していること

### 5.2 デバッグ設定

- `WP_DEBUG`, `WP_DEBUG_LOG` は本番でOFF
- PHP Warning / Notice が表示されないこと

---

## 6. 表示確認ポイント

- Hero
- About（3席種）
- Menu（3カテゴリ）
- Access
- News
- Footer
- `meal` / `drink` / `dessert` の順序が保持されていること
- 地図オーバーレイのボタン文言が `Google Maps` になっていること
- SNS / OGP / 構造化データに欠落がないこと

---

## 7. 移行手順

### 7-1. WordPress管理画面へログイン

### 7-2. FileZilla で接続

- `wordpress`
- `wp-config.php`
- `hanacafe/wp`

### 7-3. ドキュメントルート確認

- `index.php`
- `hanacafe/.htaccess`

### 7-4. テーマ配置

- `hanacafe/wp/wp-content/themes/hanacafe-theme`

### 7-5. uploads 配置

- `hanacafe/wp/wp-content/uploads`

### 7-6. 本番URL確認

- `https://intp.site/2999/WordPress/hanacafe`

### 7-7. DB確認

- `hc`

### 7-8. All-in-One WP Migration エクスポート

### 7-9. All-in-One WP Migration インポート

### 7-10. URL置換確認

- `http://hanacafe.local` → `https://intp.site/2999/WordPress/hanacafe`

### 7-11. パーマリンク保存

### 7-12. `.wpress` バックアップ保管

### 7-13. All-in-One WP Migration の上書き確認

### 7-14. 表示確認

| 確認項目 | 内容 |
|---|---|
| Home | 表示崩れがない |
| Concept | 表示される |
| News | 一覧・詳細が表示される |
| Menu | `meal` / `drink` / `dessert` の3カテゴリが表示される |
| Access | 地図・営業時間・電話番号が表示される |
| Footer | 住所・電話番号・SNSが表示される |

### 7-15. 画像確認

- 画像切れが2件以上ないこと

### 7-16. テーマ確認

- `hanacafe-theme`

### 7-17. キャッシュOFF確認

### 7-18. `wp-config.php` の権限確認

- 400 系エラーが出ないこと

### 7-19. `wp/.htaccess` と `wp-config.php` の存在確認

### 7-20. Canonical URL確認

### 7-21. URL末尾 `/wp` 混入確認

### 7-22. SNS / OGP確認

### 7-23. noindex確認

### 7-24. Google Search Console 登録

---

_最終更新: 2026-04-08 | v1.0.1 | HanaCAFE_
